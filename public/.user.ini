; Default PHP limits (2M upload / 8M post) are too small for real photo uploads
; (multi-file CMS gallery: up to 10 images) and silently drop the whole request
; body with no error surfaced to the app. Picked up automatically by PHP-FPM/CGI.
upload_max_filesize = 10M
post_max_size = 90M
